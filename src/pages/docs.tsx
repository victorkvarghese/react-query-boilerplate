import { Layout } from '../components/Layout';

const Docs: React.FC = () => (
  <Layout>
    <div style={{ display: 'flex', flex: 1, background: 'rgb(36, 40, 47)', minHeight: '100vh' }}>Docs</div>
  </Layout>
);

export default Docs;
